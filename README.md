# tChrome

**把你正在使用的 Chrome，变成一个能观察、操作、记住和收藏资料的 AI 助手。**

tChrome 运行在 Chrome 侧栏。你描述目标，Agent 读取页面、选择工具、执行操作，再根据结果决定下一步。它可以连续处理多步任务，直到完成、需要你补充信息、你主动停止，或遇到无法继续的错误。

它由 **Chrome 扩展 + 本机 Bun 服务 + 模型服务** 组成。扩展操作浏览器，本机服务管理任务和数据，模型负责判断下一步。适合希望在日常浏览器里使用和改造个人 Agent 的用户与开发者。

## 能做什么

| 场景 | 可以交给它的任务 |
| --- | --- |
| 阅读与整理 | 总结当前页面，提取表格，查找关键内容，对比多个网站的信息 |
| 网页操作 | 打开页面、切换标签、点击、输入、滚动、填写表单，结合页面反馈完成多步操作 |
| 搜索与调研 | 搜索资料、进入来源页面核对，把发现整理成回复或收藏条目 |
| 页面检查 | 读取 DOM、可访问性信息和元素状态，截图、执行页面 JavaScript，辅助检查网页行为 |
| 浏览器管理 | 使用对应工具管理标签页、下载、Cookie 和页面存储，处理原生对话框 |
| 本机协作 | 读写文件、搜索目录、执行命令、管理后台进程，以及用系统应用打开文件 |
| 资料收藏 | 把有用的网站、账号和普通资料存入本地资料库，在侧栏查看、搜索、编辑、复制或删除 |
| 连续协作 | 在同一会话中继续任务，使用会话记忆；将长期信息保留给后续会话 |

这些是已接入的工具能力和使用场景。具体任务效果仍取决于模型、网页结构、浏览器权限和网络情况。

你可以这样开始：

> 看一下当前页面，整理主要内容和需要注意的地方。
>
> 找几个适合学习 WebGL 的网站，核对后把值得收藏的存到资料库，备注各自用途。
>
> 检查这个表单的必填项和错误提示，执行后告诉我实际观察到了什么。
>
> 把刚才确认的信息记在本会话里，接下来继续比较第二个方案。

## 优势在哪里

### 1. 直接围绕你正在看的网页工作

发消息时，侧栏会带上当前标签信息；Agent 再按需读取页面内容。浏览器操作发生在你正在使用的 Chrome 中，可以利用已有页面和登录状态，处理过程也能直接看到。

不必先为每个任务写脚本或搭流程。对于路径不确定、需要观察后再判断的网页任务，可以先说清楚目标，再根据实际结果推进。

### 2. 有执行循环，也有可见的过程

模型通过工具调用执行操作，Runtime 负责队列、状态、记录和停止。正常工具循环没有固定的“调用 20 次就停”上限；遇到连续无效模型提交等错误仍会终止。

面板显示操作理由和最终结果，等待时显示点状动效，压缩时显示当前阶段。工具执行异常会作为失败结果交回模型，便于纠正下一次调用；服务请求错误与任务执行状态分开显示。

### 3. 工具多，但按需加载

启动时只提供常驻工具、核心网页工具和工具发现入口。其他浏览器、网络、本机及资料库工具由 Agent 按需加载，让每次请求只携带当前已启用工具的完整定义。

这减少了初始工具说明的负担；需要更多能力时再展开，也方便继续添加自己的工具。

### 4. 长会话有压缩，也保留原始记录

System 与 User 使用独立模块，由 Runtime 组装。主模型请求发送前，文本达到 **200,000 字符** 时进入模型压缩流程；这是字符阈值，不是 200K token，也不包含独立的工具 schema。压缩后仍超过 **250,000 字符**时，统一优先将 notes 正文完整保存在本地 `context-files/`，模型收到绝对路径引用，需要时按字节分页读取。笔记和项目记忆累计过大也适用，不删除保存的原文。

压缩按轮次整理“用户要什么、做了什么、结果如何”，先处理较早历史，必要时处理当前轮较早的工具批次和已有摘要，保留近期上下文。压缩期间当前任务等待，完成后继续。

原始记录保存在本机。查询 Agent 可从指定摘要来源中选择相关轮次，Runtime 再返回对应模块的原始内容；单次结果最多 2,000 字符，支持继续分页。它提供回查机制，不能保证摘要或语义匹配永远无遗漏。

### 5. 记忆与资料库各司其职

| 内容 | 用途 | 保留范围 |
| --- | --- | --- |
| 工作笔记 | 草稿、候选项和中间材料 | 当前会话 |
| 会话记忆 | 本会话已确认的事实、偏好和决定 | 当前会话，跨轮保留 |
| 长期记忆 | 后续会话仍有用的信息 | 跨会话共享 |
| 资料库 | 人可以直接查看和管理的网站、账号、普通资料 | 跨会话共享 |

资料库不会自动整库塞进模型上下文。Agent 通过动态工具 `library` 查询或修改，面板与工具使用同一份存储。删除会话不会删除资料库或共享长期记忆。

### 6. 数据在本机，实现可以自己改

会话、工具结果、模型交互日志、截图、记忆和资料库均由本机服务保存。提示词、工具定义、模型适配和面板代码都在仓库中，方便检查过程和调整行为。

**本地保存不等于离线推理**：进入模型上下文的消息、页面内容、工具结果及图片会发送给所选模型服务。

## 和主流方案怎么选

以下按常见使用方式比较产品定位，不是性能评测；这些方案也可以通过开发或集成扩展能力。

| 方案 | 常见使用方式 | 更适合的需求 | 与 tChrome 的取舍 |
| --- | --- | --- | --- |
| 普通聊天助手（以聊天、搜索为主的模式） | 对话、检索、分析和生成内容 | 写作、问答、思路整理 | tChrome 重点接通当前 Chrome 页面操作、本机工具和资料保存；普通聊天模式通常更容易直接上手 |
| [browser-use](https://github.com/browser-use/browser-use) 等浏览器 Agent 框架 | 在代码中配置模型、浏览器和 Agent 任务 | 开发自己的浏览器 Agent、集成到应用 | tChrome 提供现成的侧栏、会话、记忆和资料库；框架更适合围绕自己的运行环境编程组合 |
| [Playwright](https://playwright.dev/) 等浏览器自动化工具 | 编写明确的定位、动作与断言 | 回归测试、可重复的自动化流程 | Playwright 更适合精确控制和验证固定步骤；tChrome 让模型根据页面反馈临时决定步骤，结果更依赖模型判断 |
| [n8n](https://n8n.io/) / [Dify](https://dify.ai/) 等工作流与 AI 应用平台 | 配置节点、工具、数据连接和流程，也可嵌入 Agent | 系统集成、可编排业务流程、AI 应用搭建 | tChrome 聚焦个人 Chrome 内的交互任务；跨系统编排、团队流程通常更适合专门的平台 |

如果你的任务是“在我当前浏览器里帮我做完，并把过程和有用资料留下来”，tChrome 值得尝试。固定步骤需要反复稳定执行时，脚本或工作流往往更容易验证和维护。

## 安装与使用

### 准备环境

- Chrome **135 或更新版本**。
- [Bun](https://bun.sh/)，用于安装依赖、启动服务和构建扩展。

当前默认数据路径和本机使用流程以 macOS 为主；其他系统的本机命令与应用打开行为需要按环境验证。

### 1. 获取代码

```bash
git clone https://github.com/huyudonglove/tChrome.git
cd tChrome
bun install
```

### 2. 构建并启动

```bash
bun run build
bun run service
```

保持服务进程运行。服务地址为 `http://127.0.0.1:18788`，可以访问 `/health` 检查本机服务和扩展连接；它不验证远程模型是否可用。

### 3. 加载扩展

1. 在 Chrome 打开 `chrome://extensions`，开启“开发者模式”。
2. 点击“加载已解压的扩展程序”，选择项目的 **`dist/`** 目录。
3. 打开希望操作的网页，点击 tChrome 扩展图标打开侧栏。
4. 输入任务。运行过程中可以点击输入框旁的停止按钮。

关闭侧栏不等于停止任务，需要结束执行时使用停止按钮。

### 常用入口

| 入口 | 用法 |
| --- | --- |
| 输入框 | Enter 发送，Shift + Enter 换行；发送后清空，可继续准备下一条草稿 |
| 会话按钮 | 新建、切换、搜索和删除会话 |
| 会话列表右下角设置 | 切换模型，选择直连或代理；选择自动保留 |
| 顶栏书签图标 | 打开资料库，查看或维护收藏条目 |
| 运行状态 | 查看处理、压缩或等待回复状态；运行中可停止 |

**收藏资料**：可以自己在资料库中新建，也可以告诉 Agent：“把这个网站存到资料库，备注适合查技术文档。”Agent 通过 `list_browser_tools` 发现 `library`，用 `catalog.add` 加载，再调用 `list/get/save/delete`。面板打开时每三秒刷新，编辑中的草稿独立保留。账号和密码按个人使用方式直接明文显示，可逐字段复制。

**连接设置**：首次启动默认直连，可以在设置中切换直连或代理，选择会自动保存。

## 更新与排查

| 情况 | 操作 |
| --- | --- |
| 更新了扩展、工具定义或面板样式 | 执行 `bun run build`，在扩展管理页重新加载 tChrome；服务端也有改动时重启服务 |
| 只修改了服务代码 | 重启 `bun run service` |
| 提示扩展与服务工具版本不一致 | 重新构建、重启服务、重新加载扩展 |
| 本机服务连接失败 | 检查服务终端与 `/health`，确认端口 `18788` 可访问 |
| 模型响应失败 | 检查连接设置和服务日志；本机健康不代表模型服务正常 |
| 任务未完成 | 查看最终错误和操作记录，修正输入或环境后重试；日志可用于定位具体失败步骤 |

### 本地数据在哪里

默认目录：

```text
~/Library/Application Support/tChrome/
  conversations/       会话账本、轮次、事件、工具原文、模型日志、截图及归档
  memory/project/      共享长期记忆
  library/items.json   网站、账号和普通资料
  connection.json      模型与代理选择
```

可通过 `TCHROME_DATA` 指定其他数据目录。某个会话的 `events.jsonl` 和 `provider.md` 可用于核对工具调用与模型交互。

## 当前边界

- 当前提供源码安装方式，需要本机服务持续运行，依赖可用的模型服务。
- 模型可能选错工具、误读页面或执行多余步骤。任务成本和速度取决于请求次数、页面及上游服务；项目没有提供跨产品成功率或费用基准。
- 截图可作为图片传给支持视觉的模型，但侧栏尚无图片上传或粘贴入口。当前页面信息来自已获取的观察，不是持续实时监控。
- 浏览器受限页面、权限、调试连接和复杂网页行为可能限制工具使用。已保存执行记录不等于支持任意断点自动续跑。
- 本机文件和命令工具具有真实副作用。`affectsPage=false` 只声明不改变页面，不表示只读；账号密码和工具日志可能含敏感内容。
- 服务仅监听本机回环地址并校验网页来源；它不是本机进程身份认证或完整沙盒。可用 `TCHROME_EXTENSION_ORIGIN=chrome-extension://<扩展ID>` 限制允许的扩展来源。

## 开发与扩展

```text
extension/
  sidepanel/           React 面板、资料库和交互
  tools/               浏览器工具执行器
service/
  runtime/             执行循环、状态、队列与持久化
  context/             分层提示词、栏目描述和上下文组装
  tools/               工具定义、注册、参数校验与执行
  agents/compression/  压缩 Agent 的提示词、流程及返回工具
  agents/query/        查询 Agent 的提示词、流程及返回工具
  context-archive/     原始来源与归档索引
  memory/              会话与长期记忆
  library/             跨会话资料库
  skills/              Skill 文件与加载清单
  images/              截图存储与模型图片输入
  provider/            模型协议适配、重试与响应解析
  presentation/        会话展示投影
```

工具定义位于 `service/tools/definitions/`；主 Agent 的提示词按 8 个 System 规则和 15 个 User 数据栏目维护，再由 Runtime 组装；System #baseTools 展示常驻能力导航，User #tools 展示本轮已加载的动态能力导航；每项由工具名和 function.description 首句生成，完整调用说明与参数 schema 通过 tools[] 发送。过程说明取工具参数 reason，最终回复和提问分别取 text、question。压缩与查询 Agent 各自维护提示词和返回工具，共用模型请求能力。

| 命令 | 用途 |
| --- | --- |
| `bun run service` | 启动本机服务 |
| `bun run build` | 构建扩展到 `dist/` |
| `bun test` | 运行自动化测试 |
| `bunx tsc --noEmit` | TypeScript 类型检查 |
| `bun run check` | 测试与扩展构建，不含独立类型检查 |

详细实现见 [服务说明](service/README.md)、[扩展说明](extension/README.md)、[资料库](service/library/README.md)、[字段协议](docs/schema.md) 与 [数据存储](docs/data.md)。

脚本统一保存在服务数据目录的 `scripts/`。Agent 先用 `script_patch` 提交单文件补丁，确认成功后，在下一次模型调用中通过 `execute_javascript` 操作页面，或用 `local.run` / `local.process_start` 在本机运行；`script_read` 和 `script_list` 用于查看和查找脚本。
