{
  "type": "function",
  "function": {
    "name": "submitTurnSummaries",
    "description": "提交当前这一轮的摘要。一次调用是一条 {tag, actions, result}；大轮可在同一次回包里调用多次，全部归属 User 材料里这一轮。userRequest 由 Runtime 从本轮用户原话填写，不要提交。",
    "parameters": {
      "type": "object",
      "properties": {
        "tag": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S",
          "description": "用于检索的主题字符串，概括关键对象、事件或约束。"
        },
        "actions": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S",
          "description": "实际执行的关键步骤、修正和重要失败，整理为一段字符串。多个步骤用文字串联，区分计划与已执行操作。"
        },
        "result": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S",
          "description": "本轮已验证结果、最终回复、错误或等待状态的文字摘要。保留回复与执行证据的差异；查询结论注明来自历史证据。"
        }
      },
      "required": ["tag", "actions", "result"],
      "additionalProperties": false
    }
  }
}
