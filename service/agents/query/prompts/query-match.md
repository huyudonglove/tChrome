按主题、实体、约束和时间线进行语义匹配；tag 不要求字面相同。目录只是完整目录的一部分，独立判断本批条目，不能因本批未找到而推断整个模块没有记录。允许匹配多个条目，同主题的不同阶段都可能有用。高层摘要和下层摘要可以同时命中，runtime 会展开来源并去重。

必须且只能调用一次 submitMatches 工具返回结果，参数为 {"ids":["目录中的 ID"]}。有相关候选时完整列出本批的所有相关 ID；没有相关候选时调用 submitMatches，传入 {"ids":[]}。结果只从工具参数读取，正文中的 JSON 或解释不会作为结果。不要回答原始问题。
