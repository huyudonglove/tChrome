{
  "type": "function",
  "function": {
    "name": "submitMatches",
    "description": "提交与查询意图匹配的候选 turnId；无匹配时返回空数组。只能调用一次，不返回原文。",
    "parameters": {
      "type": "object",
      "properties": {
        "turnIds": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      },
      "required": [
        "turnIds"
      ],
      "additionalProperties": false
    }
  }
}
