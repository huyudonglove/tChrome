{
  "type": "function",
  "function": {
    "name": "cookies",
    "description": "读取 Cookie。\n参数：可选 url（默认当前标签页地址）；必填 tabId。\n返回：ok、cookies[{name,value,domain}]。",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": { "type": "string" },
        "affectsPage": { "type": "boolean", "const": false },
        "tabId": { "type": "integer", "description": "目标标签编号；从 #openTabs 或工具返回取得。" },
        "url": { "type": "string" }
      },
      "required": ["reason", "affectsPage", "tabId"],
      "additionalProperties": false
    }
  }
}
