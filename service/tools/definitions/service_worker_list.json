{
  "type": "function",
  "function": {
    "name": "service_worker_list",
    "description": "列出目标网页所属源可见的 Service Worker 注册信息，需要页面支持 Service Worker API。\n返回：ok、serviceWorkers（scope、active、installing、waiting 脚本地址）。",
    "parameters": {
      "type": "object",
      "properties": {
        "tab": {
          "type": "number"
        },
        "reason": {
          "type": "string"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage"
      ],
      "additionalProperties": true
    }
  }
}
