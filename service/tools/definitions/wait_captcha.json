{
  "type": "function",
  "function": {
    "name": "wait_captcha",
    "description": "等待检测到 Turnstile、reCAPTCHA 或 hCaptcha 出现，最多 12 秒。出现即返回，不等待验证通过；超时 ok=false。返回 present、type、status、clickable。 必填 tabId（标签编号）、timeoutMs（总超时毫秒，1～12000）。 多个验证码或响应字段返回 ambiguous=true，不宣称全部通过。",
    "parameters": {
      "type": "object",
      "properties": {
        "tabId": {
          "type": "integer",
          "description": "目标标签编号；从 #openTabs 或工具返回取得。必须显式指定，不随前台切换；目标失效时返回错误。"
        },
        "reason": {
          "type": "string"
        },
        "affectsPage": {
          "type": "boolean"
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 1,
          "maximum": 12000
        }
      },
      "required": [
        "reason",
        "affectsPage",
        "tabId"
      ],
      "additionalProperties": true
    }
  }
}
