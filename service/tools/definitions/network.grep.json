{
  "type": "function",
  "function": {
    "name": "network.grep",
    "description": "等待匹配网络响应，并在响应体中检索关键字（返回关键字附近上下文）。\n参数：必填 urlContains、keyword、tabId；可选 status、contextChars（默认 400）、ms。\n返回：ok、requestId、url、status、matches[{offset,before,hit,after}]、totalChars。\n执行调度：parallel（Runtime 固定，不必返回）。",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": {
          "type": "string"
        },
        "tabId": {
          "type": "integer"
        },
        "urlContains": {
          "type": "string"
        },
        "keyword": {
          "type": "string"
        },
        "status": {
          "type": "integer"
        },
        "contextChars": {
          "type": "integer"
        },
        "ms": {
          "type": "integer"
        }
      },
      "required": [
        "reason",
        "tabId",
        "urlContains",
        "keyword"
      ],
      "additionalProperties": false
    }
  },
  "execution": "parallel"
}
