{
  "type": "function",
  "function": {
    "name": "wait_network",
    "description": "等到页面接口和资源安静后再读页。\n参数：可选 ms（安静多久）；必填 tabId（目标标签编号）。\n返回：ok、requests（请求数量）、sample、title（页面标题）、url（页面地址）、text（返回文本）；超时 ok=false。",
    "parameters": {
      "type": "object",
      "properties": {
        "ms": {
          "type": "number",
          "description": "连续安静多久算就绪"
        },
        "tabId": {
          "type": "integer",
          "description": "目标标签编号；从 <openTabs> 或工具返回取得。必须显式指定，不随前台切换；目标失效时返回错误。"
        },
        "reason": {
          "type": "string"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage",
        "tabId"
      ]
    }
  }
}
