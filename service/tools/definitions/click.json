{
  "type": "function",
  "function": {
    "name": "click",
    "description": "按元素编号或目标文字点击控件。\n提供 ref/id 或 targetText 至少一个；同时提供时优先编号。编号为 page.* 或 snapshot 返回的 e_。targetText 是要点击的控件文字，不是行动说明。文字匹配多个控件时先用 list/snapshot 取得 e_ 再点击。\n例：{\"tabId\":123,\"ref\":\"e_05\"}。必填 tabId。\n返回：ok、clicked。\n执行调度：serial（Runtime 固定，不必返回）。",
    "parameters": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "page.* 或 snapshot 返回的 e_ 编号",
          "pattern": "^(?:e|r)_[0-9]{2,}$",
          "minLength": 1
        },
        "tabId": {
          "type": "integer",
          "description": "目标标签编号；从 <openTabs> 或工具返回取得。必须显式指定，不随前台切换；目标失效时返回错误。"
        },
        "reason": {
          "type": "string",
          "description": "可选的行动说明，不用于定位控件。"
        },
        "targetText": {
          "type": "string",
          "description": "要点击的控件可见文字、value 或 aria-label；必须是非空字符串。",
          "minLength": 1
        }
      },
      "required": [
        "tabId"
      ],
      "anyOf": [
        {
          "required": [
            "ref"
          ],
          "type": "object",
          "properties": {
            "ref": {
              "type": "string",
              "description": "page.* 或 snapshot 返回的 e_ 编号",
              "pattern": "^(?:e|r)_[0-9]{2,}$",
              "minLength": 1
            }
          }
        },
        {
          "required": [
            "targetText"
          ],
          "type": "object",
          "properties": {
            "targetText": {
              "type": "string",
              "description": "要点击的控件可见文字、value 或 aria-label；必须是非空字符串。",
              "minLength": 1
            }
          }
        }
      ],
      "additionalProperties": false
    }
  },
  "execution": "serial"
}
