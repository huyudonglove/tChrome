{
  "type": "function",
  "function": {
    "name": "set_cookie",
    "description": "设置 Cookie。\n参数：name（名称）、value（值），可选 url（网址）/domain（域名）/path（路径）/secure（是否仅通过安全连接发送）/sameSite（跨站发送限制）/expires（过期时间）。\n返回：ok、name、value（当前值）。",
    "parameters": {
      "type": "object",
      "properties": {
        "tab": {
          "type": "number"
        },
        "reason": {
          "type": "string"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage"
      ],
      "additionalProperties": true
    }
  }
}
