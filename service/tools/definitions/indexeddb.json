{
  "type": "function",
  "function": {
    "name": "indexeddb",
    "description": "读写页内 IndexedDB。\n参数：action=read|write|delete|list；db 必填（list stores 可只给 db）；read/write/delete 建议 store+key；write 可带 value。必填 tabId。\n返回：ok、value/keys/stores。",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": { "type": "string" },
        "affectsPage": { "type": "boolean" },
        "tabId": { "type": "integer", "description": "目标标签编号；从 <openTabs> 或工具返回取得。" },
        "action": { "type": "string", "enum": ["read", "write", "delete", "list"] },
        "db": { "type": "string", "description": "数据库名" },
        "store": { "type": "string", "description": "object store 名" },
        "key": { "type": "string" },
        "value": { "type": "string", "description": "write 时写入的 JSON 字符串" }
      },
      "required": ["reason", "affectsPage", "tabId"],
      "additionalProperties": false
    }
  }
}
