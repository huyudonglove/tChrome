{
  "type": "function",
  "function": {
    "name": "scroll",
    "description": "滚动当前页。\n参数：可选 amount（滚动像素数，负数向上）、tab（目标标签编号）。\n返回：ok。",
    "parameters": {
      "type": "object",
      "properties": {
        "amount": {
          "type": "number",
          "description": "像素，负数向上"
        },
        "tab": {
          "type": "number"
        },
        "reason": {
          "type": "string"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage"
      ]
    }
  }
}
