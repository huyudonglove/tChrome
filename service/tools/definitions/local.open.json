{
  "type": "function",
  "function": {
    "name": "local.open",
    "description": "使用系统默认应用打开本机绝对路径。成功表示启动请求成功，不代表已验证应用显示内容。\n执行调度：serial（Runtime 固定，不必返回）。",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string"
        },
        "reason": {
          "type": "string",
          "description": "本次操作的目的"
        }
      },
      "required": [
        "path",
        "reason"
      ],
      "additionalProperties": false
    }
  },
  "execution": "serial"
}
