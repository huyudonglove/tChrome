{
  "type": "function",
  "function": {
    "name": "page.list_interactive_elements",
    "description": "列视口内可交互元素（A11y 投影）。每条：id（e_，可直接 click/wait）、role（标准化）、name、states{enabled,checked,selected,expanded,invalid,…}、tag、inView、regionId。role 含 div/span 模拟控件（有 role 或可访问名时）。可按 regionId 收窄。必填 tabId。",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": { "type": "string" },
        "affectsPage": { "type": "boolean" },
        "tabId": { "type": "integer" },
        "regionId": { "type": "string" }
      },
      "required": ["reason", "affectsPage", "tabId"],
      "additionalProperties": false
    }
  }
}
