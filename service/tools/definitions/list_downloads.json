{
  "type": "function",
  "function": {
    "name": "list_downloads",
    "description": "列出下载项。\n返回：ok、downloads（下载记录）。\n执行调度：parallel（Runtime 固定，不必返回）。",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "reason"
      ],
      "additionalProperties": true
    }
  },
  "execution": "parallel"
}
