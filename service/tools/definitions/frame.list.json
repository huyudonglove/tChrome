{
  "type": "function",
  "function": {
    "name": "frame.list",
    "description": "列出页面 frame 树（含 iframe）。用于取得 frameId，供 page.* / wait 等工具传 frameId 定位到内嵌文档。\n执行调度：parallel（Runtime 固定，不必返回）。",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": {
          "type": "string"
        },
        "tabId": {
          "type": "integer"
        }
      },
      "required": [
        "reason",
        "tabId"
      ],
      "additionalProperties": false
    }
  },
  "execution": "parallel"
}
