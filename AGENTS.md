# 项目协作约束

## 按已确认的设计实现

- 用户确认的设计是实现依据。不得以“优化”“健壮性”或“保护”为由，擅自增加或改变门禁、阈值、兜底、兼容分支、分片、重试层、状态或执行阶段。
- 发现设计无法直接落地时，先说明具体证据、原因、拟议方案及行为影响；获得用户确认后再改变设计。常规实现细节在不改变既定行为的范围内自行处理。
- 修复问题应回到既定设计，删除偏离设计的旧逻辑，不叠加新的补救分支来维持错误实现。
- 交付时区分“实现既定设计”与“改变行为”，明确列出新增或变更的执行规则；不得将行为变化隐藏在重构或优化中。
- 检查实现时沿实际调用链核对内部行为，不能只确认外层入口符合设计。尤其检查是否在下层重复设置限制、拆分请求或增加重试。

## 已确认的具体边界

- 压缩入口对本次选中的材料一次发送、一次返回；不得恢复内部 60K 分批、8K 字段分片或递归摘要合并。外层现有 history/current/summaries 阶段与传输层重试需分别说明，不能混称为“一次压缩只请求一次”。
- 保留常驻 baseTools 与动态 tools 的模块划分，以及模型可见的编号规则表；整理描述不等于删除能力。
- 不擅自增加参数兜底、旧字段兼容或定位参数强转。
- 验证应与风险相称，优先维护已有测试；仅为实际故障或必要行为补回归用例，避免重复断言、锁文案测试和无需求的泛化测试。

以上约束的增删应以用户后续明确要求为准。
