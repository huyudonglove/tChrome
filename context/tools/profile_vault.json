{
  "type": "function",
  "function": {
    "name": "profile_vault",
    "description": "浏览器配置库。\n参数：action（save/get/list）、可选 profileId（配置编号）、data（数据）。\n返回：ok（是否成功）、profile/items（结果列表）。\naffectsPage=false。",
    "parameters": {
      "type": "object",
      "properties": {
        "tab": {
          "type": "number"
        },
        "reason": {
          "type": "string",
          "description": "面向用户的行动理由，表达要求见 #output。"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage"
      ],
      "additionalProperties": true
    }
  }
}
