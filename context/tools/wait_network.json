{
  "type": "function",
  "function": {
    "name": "wait_network",
    "description": "等到页面接口和资源安静后再读页。\n参数：可选 ms（安静多久）、tab（目标标签编号）。\n返回：ok（是否成功）、requests（请求数量）、sample、title（页面标题）、url（页面地址）、text（返回文本）；超时 ok=false。\naffectsPage=false。",
    "parameters": {
      "type": "object",
      "properties": {
        "ms": {
          "type": "number",
          "description": "连续安静多久算就绪"
        },
        "tab": {
          "type": "number"
        },
        "reason": {
          "type": "string",
          "description": "面向用户的行动理由，表达要求见 #output。"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage"
      ]
    }
  }
}
