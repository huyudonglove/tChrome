{
  "type": "function",
  "function": {
    "name": "api_execute",
    "description": "调用已登记接口。\n参数：url（网址），可选 method（请求方法）、headers（请求头）、body（请求正文）。\n返回：ok（是否成功）、status（响应状态码）、text（返回文本）。\naffectsPage=false。",
    "parameters": {
      "type": "object",
      "properties": {
        "tab": {
          "type": "number"
        },
        "reason": {
          "type": "string"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage"
      ],
      "additionalProperties": true
    }
  }
}
