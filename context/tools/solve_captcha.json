{
  "type": "function",
  "function": {
    "name": "solve_captcha",
    "description": "点当前页 Turnstile、reCAPTCHA 或 hCaptcha 的内嵌验证页面。\n参数：可选 tab（目标标签编号）。\n返回：ok（是否成功）、type（类型）；认不出或不点得动则普通失败，拼图滑块改 screenshot 后 calibrate_drag。\naffectsPage=true。",
    "parameters": {
      "type": "object",
      "properties": {
        "tab": {
          "type": "number"
        },
        "reason": {
          "type": "string"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage"
      ],
      "additionalProperties": true
    }
  }
}
