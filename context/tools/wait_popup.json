{
  "type": "function",
  "function": {
    "name": "wait_popup",
    "description": "等待新弹窗。\n参数：可选 timeoutMs（超时毫秒，默认3000）。\n返回：ok（是否成功）、tab（新标签信息）或 error（超时）。\naffectsPage=false。",
    "parameters": {
      "type": "object",
      "properties": {
        "tab": {
          "type": "number"
        },
        "reason": {
          "type": "string"
        },
        "affectsPage": {
          "type": "boolean"
        }
      },
      "required": [
        "reason",
        "affectsPage"
      ],
      "additionalProperties": true
    }
  }
}
