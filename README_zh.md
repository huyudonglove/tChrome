# tChrome · 驭舟 (Helm)

[English](README.md) | [简体中文](README_zh.md)

**在日常 Chrome 中为你掌舵、观察、操作与沉淀记忆的自主 AI 伙伴。**

https://github.com/user-attachments/assets/a5b5e1a3-2cf6-4b94-9201-a52ef6cb4404

*实录演示：Helm 在日常真实 Chrome 浏览器中自主观察、多步规划并攻克复杂交互挑战（WebGames 农夫过河评测基准）。*

---

### 你好，我是驭舟（Helm）

我寄宿在你的 Chrome 侧栏中，以你熟悉的真实浏览器为窗口，向下打通本机系统，向上连接大语言模型。

我不是一个仅停留在对话框里的文字聊天机器人，而是一个**能够替你观察网页、操作控件、管理标签、感知宿主环境并调度本机终端的自主浏览器智能体（Autonomous Browser Agent）**。

在与你的协作中，我秉持以下原则：
- **不凭空假设，严谨取证**：我不会盲目推测网页状态。每走一步，我都会基于浏览器内核的 A11y 树、精准状态探针或高清视觉切片获取反馈；未经验证，我不宣称成功。
- **目标驱动，层次分明**：面对复杂长流程任务，我会将总目标自顶向下拆解为结构化子目标，阶段切换清晰，保留完整历史轨迹。
- **分层认知，本地优先**：草稿放工作笔记、事实存会话记忆、经验入长期记忆、网址与账号存本地资料库。你所有的运行数据、日志、截图和资料库全部完整保存在你的本机电脑上，开放透明，完全由你掌控。

**tChrome** 是我的躯干与工程载体（由 **Chrome 扩展 + 本机 Bun 服务 + 模型服务** 组成），而我——**Helm（驭舟）**，负责在网页与数字世界中为你把握方向、执行行动、达成所托。

---

## 能做什么

| 场景 | 可以交给我的任务 |
| --- | --- |
| **阅读与深度整理** | 总结当前页面要点，提取复杂表格，在多标签页间交叉对比信息，定位长文档关键细节 |
| **全闭环网页操作** | 打开网址、切换/分组标签、基于 A11y 语义树与复合工具实现点击、输入、表单连贯录入与提交等待 |
| **多源调研与核对** | 自主搜索资料、深入来源网页核对事实，将关键发现提炼为报告或沉淀进本地资料库 |
| **前端与页面自检** | 读取内核无障碍树（A11y）、断言控件状态（checked/expanded/invalid）、局部高清截屏、执行脚本辅助诊断 |
| **宿主环境感知** | 突破扩展沙箱隔离，直接感知 Chrome 本地配置文件（内存休眠策略、网站权限白名单、静默下载配置） |
| **视觉标记与定位** | 针对纯图形、Canvas 或密集控件场景生成视口 SoM 标记图（Set-of-Marks），离散化精准点击交互 |
| **本机跨界协作** | 读写本地文件、检索目录、执行 Shell 脚本、管理后台进程，以及调用系统应用打开产物 |
| **长效资产沉淀** | 将高价值网站、账号与结构化资料一键存入本地资料库，在侧栏随时查看、检索或复用 |
| **长程自主演进** | 在多轮对话中自动运行独立压缩岗与查询岗，跨轮次沉淀会话事实，跨会话保留演进经验 |

### 你可以这样唤醒我：

> “看一下当前页面，整理出它的核心亮点以及需要特别注意的使用限制。”
>
> “去搜一下最近热门的 WebGPU 开源学习项目，逐个打开看 README，提炼出主要特色与 Star 数，帮我存入资料库。”
>
> “检查当前注册表单在各种边界输入下的校验提示，执行后把实际观察到的报错截图记录下来。”
>
> “把刚才确认的配置项记在本会话里，接下来帮我继续分析第二套对比方案。”

---

## 核心设计与工程优势

### 1. 立足真实工作环境，而非无头孤岛 (Real Chrome Workspace)
我不跑在隔离的无头（Headless）浏览器容器里，而是**直接运行在你日常登录好账号、带有 Cookie 和浏览历史的真实 Chrome 浏览器中**。
你可以随时看到我点击了哪里、输入了什么，也可以在任何需要人工介入的关键时刻直接接管。对于路径多变、需要即时探索的复杂任务，无需预先编排固定脚本。

### 2. 纯 A11y 内核语义全链路闭环 (Accessibility-First Architecture)
传统浏览器自动化依赖脆弱的 DOM 选择器、XPath 或高昂的纯视觉坐标点击，极易受样式重构、动态混淆与渲染形变干扰。tChrome 全面转向 Chromium 核心无障碍树（Accessibility Tree）：
- **物理级脱水过滤**：过滤成千上万无意义的样式 `<div>` 与装饰节点，将界面纯净提炼为 `Role`（职责）、`Name`（意图）与 `States`（状态），上下文信噪比跃升，Token 消耗直降 80% 以上。
- **全链路严密闭环**：
  - **感知**：`page.list_interactive_elements` 投影当前视口可交互元素及其细粒度状态（`enabled`, `checked`, `expanded`, `selected` 等）；
  - **等待**：`wait` 原生支持基于 `role + name + states` 的条件等待，彻底告别盲目固定延时；
  - **操作**：`page.click_role` / `page.fill_role` / `page.select_role` 一步命中目标；
  - **验收**：`page.assert` 与 `page.recheck` 提供细粒度状态断言（如等待提交后按钮变为 `expanded=false` 或表单字段 `invalid=false`），形成牢不可破的执行证据链。

### 3. 宿主与内核双向贯通 (Host Environment Perception)
当扩展沙箱因特权安全策略被限制访问 `chrome://settings` 等特权页面时，tChrome 能够无缝切换为**宿主机物理视角**：
- 通过服务层原生工具直读本地 Chrome 配置文件（macOS / Linux / Windows 上的 `Preferences` 与 `Local State` JSON 文件）；
- 自动探查并自检**后台标签休眠策略（Memory Saver）**域名白名单、**站点权限白名单（剪贴板/弹窗拦截）**以及**静默下载配置**，打破纯扩展 Agent 的“井底视野”，实现物理宿主与渲染内核的双向协同。

### 4. 视觉-语义双模自适应协同 (Hybrid Vision-DOM Engine)
不同于 Computer Use 式“每步必截全量高清大图”带来的带宽雪崩、Token 膨胀与注意力稀释，tChrome 践行“语义为主、视觉探针按需介入”的高效哲学：
- **局部元素高清切片 (Crop-on-Demand)**：`capture_page(mode="element", ref=...)` 直接提取指定控件的局部高精度切片，在省去 90% 图片 Token 的同时保证模型看清微小文字与复杂图表；
- **视口 Set-of-Marks (SoM) 增强**：`capture_page(mode="som")` 在面对纯图形、游戏或密集网格界面时，自动生成离散编号标记图，将高幻觉的连续像素坐标回归转化为高精度的序号分类选择；
- **瞬态图回填机制 (Ephemeral Frames)**：新截图仅随当前工具批次精准回填，历史轮次自动剥离降级为本地磁盘路径，彻底规避多图引起的视觉疲劳与上下文胀死。

### 5. 高频复合原子工具 (Compound Tools)，交互轮次腰斩
在处理表单录入、下拉选择、点击提交并等待接口响应等长链路时，过去往往需要多次与模型往返。现在，内置的原子级复合工具实现了高内聚流转：
- `page.click_role` / `page.click_text`：A11y 角色或可见文本精确定位 + 自动点击 + 响应/文本等待，一步完成；
- `page.fill_role` / `page.select_role`：智能定位后直接输入文本或切换下拉选项；
- `page.submit_wait` / `page.fill_submit`：支持整表批量填报并挂载网络响应监听（`wait_response`）。
这不仅将操作延迟和 Token 消耗压缩了 50% 以上，更从协议层彻底消除了页面加载与事件监听的时序竞态（Race Condition）。

### 6. 确定性状态总线与精细上下文治理 (Context Assembly & 4000 Guardrail)
系统彻底抛弃了脆弱的单体 Prompt 拼接与简单的线性 message 数组：
- **分层装配总线**：将不变的 System 语义契约与流动的 User 状态槽位（Goal / Checklist / Notes / Memory / ToolIO）严谨分离，每轮根据任务状态动态组装；
- **4000 字符内联安全门禁**：所有单次工具返回与大观察均经过 4000 字符安全检查，超量结果自动外置本地并生成行宽固定的索引，主 Agent 可通过 `evidence.search` 按需检索片段或分行读取，物理级防御上下文撑爆与信噪比退化；
- **结构化短 ID 与排队引擎**：自增唯一的 `call_01`、`e_01`、`proc_01` 贯穿始终，杜绝模型随机伪造 ID 引发的级联故障。

### 7. 侧栏可交互小组件与双轨沙箱 (Interactive Widgets)
受限于浏览器扩展严格的内容安全策略（CSP），普通的内联 JavaScript 无法直接执行。为了让你在侧栏获得更加生动、富交互的体验，tChrome 引入了双轨制富交互体系：
- **轻量无脚本交互**：借助 `data-action="pick"`（随机抽取）、`data-action="count"`（计数器累加）、`data-copy` 等声明式属性，无需 JS 即可在侧栏实现即时动效与复制；
- **独立组件沙箱 (`<tchrome-widget>`)**：复杂的可交互微应用（如动态计算器、可视化看板、自包含小游戏）会被安全托管在本地独立沙箱 iframe 中，不受扩展 CSP 束缚，既保证了扩展宿主安全，又赋予了我交付全功能 Web 应用的可能。

### 8. 独立压缩岗与原文无损回查 (Lossless Compression & Query Agent)
当单会话上下文接近阈值（200,000 字符）时，Runtime 会调度独立的**压缩 Agent** 对历史轮次进行结构化提炼（意图、行动、客观结果），平滑保留最近上下文。
所有原始交互日志与工具细节均完整保留在本地底层；需要深入考证时，**查询 Agent** 可按需精准回查原文，在控制 Token 成本的同时做到历史不失真。

### 9. 分层认知体系与结构化资料库各司其职
| 存储层级 | 职能定位 | 作用范围 |
| --- | --- | --- |
| **执行清单 (`<checklist>`)** | 当前轮次步骤拆解与即时执行状态追踪 | 当轮有效，结束后自动清空 |
| **工作笔记 (`<notes>`)** | 任务推进中的临时草稿、候选集与中间过程材料 | 当前会话有效 |
| **会话记忆 (`<conversationMemory>`)** | 当前任务中已确认的事实、用户偏好与关键决定 | 当前会话跨轮次保留 |
| **长期记忆 (`<projectMemory>`)** | 跨任务适用的长期知识、系统习惯与背景共识 | 所有会话全局共享 |
| **本地资料库 (Library)** | 人机共用的结构化资产（网站收藏、账号凭证、常规资料） | 独立本地存储，侧栏直观管理 |

### 10. 数据本地优先，透明可控
会话记录、工具日志、交互详情、视觉截图、记忆与资料库均存储在你的本机。所有系统提示词、工具实现、模型适配代码完全开源。
*注：本地优先强调的是数据的持久化归属与控制权，模型推理仍会向你配置的大模型服务发送当前轮次必要的上下文。*

---

## 与主流方案的定位对比

| 方案 | 核心定位与常见用法 | 适用场景 | 与 tChrome / Helm 的取舍 |
| --- | --- | --- | --- |
| **普通聊天助手** (ChatBot / Search AI) | 对话、检索、写作、思路答疑 | 文本生成、通用咨询、无需操作浏览器的场景 | Helm 核心聚焦在**真实浏览器操作、多步任务闭环与本机联动**；纯文本问答类任务传统聊天更轻量。 |
| **browser-use 等开源框架** | Python/Node 编程框架，在代码中编排 Agent | 开发者自主构建独立浏览器 Agent 应用 | tChrome 开箱即用，提供完整的 Chrome 侧栏交互、A11y 闭环、会话记忆与本地资料库；框架更适合作为底层组件二次开发。 |
| **Computer Use 纯视觉流** | 截屏看像素，模拟鼠标键盘坐标点击 | 操作系统桌面应用、无 DOM 结构的闭源软件 | 纯视觉流依赖高频全屏推流，Token 与延迟极高；Helm 在 Web 场景以 **A11y 语义树为主、按需 SoM/局部切片为辅**，效率与稳定性显著胜出。 |
| **Playwright / Puppeteer 自动化** | 编写固定选择器、动作序列与断言 | 严格可重复的回归测试、爬虫、确定流程自动化 | Playwright 适合确定性高、步骤固定的流程；Helm 适合**路径未知、依赖实时视觉与 A11y 反馈随机应变**的复杂探索任务。 |
| **n8n / Dify / 流程编排平台** | 可视化节点流、API 连接器、流程自动化 | 跨企业系统的数据流打通与后台自动化任务 | 平台更擅长后端多系统集成；Helm 专注于**个人在日常浏览器桌面环境下的沉浸式实时协同**。 |

---

## 快速安装与使用

### 运行环境准备
- **Google Chrome 135** 或更高版本。
- **[Bun](https://bun.sh/)**：用于依赖安装、编译扩展与驱动本机服务。

> *当前默认文件路径与本机系统能力以 macOS 为最佳体验；Linux / Windows 环境亦可支持核心功能。*

### 1. 克隆代码与依赖安装
```bash
git clone https://github.com/huyudonglove/tChrome.git
cd tChrome
bun install
```

### 2. 编译并启动本机服务
```bash
bun run build
bun run service
```
本机服务默认监听 `http://127.0.0.1:18788`，可访问 `http://127.0.0.1:18788/health` 检查服务连通性。

### 3. 加载 Chrome 侧栏扩展
1. 打开 Chrome 访问 `chrome://extensions/`，右上角开启 **“开发者模式”**。
2. 点击 **“加载已解压的扩展程序”**，选中项目中的 **`dist/`** 目录。
3. 打开任意网页，点击扩展栏的 tChrome 图标唤起侧栏。
4. 在侧栏设置中配置好你的模型 API Key 与端点，即可开始与 Helm 协作！

---

## 目录结构概览

```text
tChrome/
├── extension/             # Chrome 侧栏扩展前端 (React + Tailwind)
│   ├── sidepanel/         # 侧栏交互界面、资料库管理面板
│   └── tools/             # 浏览器宿主端工具执行适配器 (A11y 投影与 CDP 联动)
├── service/               # 本机 Bun 核心服务
│   ├── runtime/           # 执行循环、任务调度、容错与状态机
│   ├── context/           # 上下文组装、System 规则与栏目编排 (4000 门禁防护)
│   ├── tools/             # 工具定义库、参数校验与注册中心 (A11y 与复合工具)
│   ├── skills/            # 领域操作知识库 (Web 观察、宿主感知、回复规范)
│   ├── agents/            # 专用子智能体 (压缩 Agent、精准回查 Agent)
│   ├── memory/            # 分层记忆引擎 (会话记忆与长期记忆)
│   ├── library/           # 本地持久化结构化资料库
│   └── provider/          # 多模型协议适配器 (OpenAI 兼容协议等)
└── docs/                  # 字段协议与数据存储设计文档
```

---

## 开源协议与交流

欢迎提交 Issue 与 Pull Request，共同打磨更懂浏览器、更省心的自主个人 Agent！
